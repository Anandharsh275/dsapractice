#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    // int n;
    // cin >> n;
    // cin.ignore();

    // string s;
    // getline(cin, s);

    // string first = s.substr(0, n);//n means how many character it will contain starting from index 0
    // string second = s.substr(n);

    // sort(first.begin(), first.end());
    // sort(second.begin(), second.end());

    // bool unlucky = true;

    // for (int i = 0; i < n; i++) {
    //     if (first[i] >= second[i]) {
    //         unlucky = false;
    //         break;
    //     }
    // }

    // if (unlucky) {
    //     cout << "YES" << "\n";
    // } else {
    //     unlucky = true; // Resetting the flag

    //     for (int i = 0; i < n; i++) {
    //         if (first[i] <= second[i]) {
    //             unlucky = false;
    //             break;
    //         }
    //     }

    //     if (unlucky) {
    //         cout << "YES" << "\n";
    //     } else {
    //         cout << "NO" << "\n";
    //     }
    // }
string k="eslkd";
cout<<k<<endl;

    return 0;
}
